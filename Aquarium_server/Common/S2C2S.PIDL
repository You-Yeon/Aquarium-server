rename cs(std::wstring, System.String);
 
global S2C2S 1000
{
    RequestLogin([in] std::wstring id, [in] std::wstring password);
 
    NotifyLoginSuccess();
    NotifyLoginFailed([in] std::wstring reason);

    JoinGameRoom([in] std::wstring id, [in] int character_num);

    Room_Appear([in] int hostID, [in] int character_num, [in] std::wstring team_color);
	Room_Disappear([in] int hostID);
}